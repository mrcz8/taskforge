{
    "name": "taskforge-backend",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "bun --hot src/server.ts",
        "start": "bun run src/server.ts",
        "test": "bun test",
        "db:migrate": "echo 'phase 0 placeholder'",
        "db:seed": "echo 'phase 0 placeholder'",
        "cli": "bun run src/cli/index.ts",
        "queue:work": "sleep infinity",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "hono": "^4.6.0"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "typescript": "^5.6.0"
    }
}